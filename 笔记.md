# AI对话 - ChatModel 

ChatModel 是最基础的概念，负责和AI大模型交互。

引入AI大模型依赖，选择阿里云大模型，提供了springboot项目的整合依赖包：

```java
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-community-dashscope-spring-boot-starter</artifactId>
    <version>1.1.0-beta7</version>
</dependency>
```



tips：在方法上按 alt+enter 选择Generate missing test methods 可以生成单元测试

```java
//最简单的测试用例
@SpringBootTest
class AiCodeHelperApplicationTests {

    @Resource
    private AiCodeHelper aiCodeHelper;

    @Test
    void chat() {
        aiCodeHelper.chat("你好，请用Java写一个Hello World程序");
    }

}
```



# 多模态 - Multimodality

多模态是指能够同时处理、理解和生成多种不同类型数据的能力，比如文本、图像、音频、视频、PDF等。

但实际上需要大模型本身支持多模态，可以参考官网中的表格[Comparison Table of all supported Language Models | LangChain4j](https://docs.langchain4j.dev/integrations/language-models/)



# 系统提示词 - SystemMessage

系统提示词是设置AI模型行为规则和角色定位的隐藏指令，用户通常不能直接看到。系统Prompt相当于给AI设定人格和能力边界，也就是告诉AI"你是谁?你能做什么?"

AI超级智能体项目-第三期 有提示词编写技巧



# AI 服务 - AI Service

AI Service 是Langchain4j 最重要的开发模式，提供了很多高层抽象的、用起来方便的API，把AI应用当做服务来开发。

区别:

- ​	越低层次的api，代码编写量越大，相对来说越自由 
- ​	越高层次的api，由于不断地封装，可以拆包即用，代码编写量小，但有一定的限制，没那么灵活



## 编写一个工厂类创建接口的实现类

方法一：

```java
@Configuration
public class AiCodeHelperServiceFactory {

    @Resource
    private ChatModel qwenChatModel;

    @Bean
    public AiCodeHelperService aiCodeHelperService() {
        //自动创建出该接口的实现类
        return AiServices.create(AiCodeHelperService.class, qwenChatModel);
    }

}
```

方法二：

```java
引入依赖：
<dependency>
    <groupId>dev.langchain4j</groupId>
    <artifactId>langchain4j-spring-boot-starter</artifactId>
    <version>1.10.0-beta18</version>
    <scope>compile</scope>
</dependency>

在接口中添加新注解@AiService：
@AiService
public interface AiCodeHelperService {

    //该注解可以直接指定文件，将内容读取出来作为提示词
    @SystemMessage(fromResource = "system-prompt.txt")
    String chat(String userMessage);
}

//@Configuration
public class AiCodeHelperServiceFactory {

    @Resource
    private ChatModel qwenChatModel;

    //AI Service的创建方法
    @Bean
    public AiCodeHelperService aiCodeHelperService() {
        //自动创建出该接口的实现类
        return AiServices.create(AiCodeHelperService.class, qwenChatModel);
    }

}
```

调用 AiServices.create 方法就可以创建出AI Service 的实现类了，背后的原理是利用Java 反射机制创建了一个实现接口的代理对象，代理对象负责输入和输出的转换，比如把String类型的用户消息参数转为UserMessage 类型并调用 ChatModel，再将AI返回的 AiMessage 类型转换为 String类型作为返回值。



**需要学习的知识点：**

**反射** 

**代理**

**执行器-（属于多线程中的）**



# 会话记忆

会话记忆默认是存储在内存中的，重启后会丢失，可以通过自定义chatMemoryStore接口的实现类，将消息保存到MySQL等其他数据源中。

```java
class PersistentChatMemoryStore implements ChatMemoryStore {

        @Override
        public List<ChatMessage> getMessages(Object memoryId) {
          // TODO: 实现通过 memory ID 从持久化存储中获取所有消息。
          // 可使用 ChatMessageDeserializer.messageFromJson(String) 
          // 和 ChatMessageDeserializer.messagesFromJson(String) 辅助方法从 JSON 反序列化。
        }

        @Override
        public void updateMessages(Object memoryId, List<ChatMessage> messages) {
            // TODO: 实现通过 memory ID 更新持久化存储中的所有消息。
            // 可使用 ChatMessageSerializer.messageToJson(ChatMessage) 
            // 和 ChatMessageSerializer.messagesToJson(List<ChatMessage>) 辅助方法将消息序列化为 JSON。
        }

        @Override
        public void deleteMessages(Object memoryId) {
          // TODO: 实现通过 memory ID 删除持久化存储中的所有消息。
        }
    }

ChatMemory chatMemory = MessageWindowChatMemory.builder()
        .id("12345")
        .maxMessages(10)
        .chatMemoryStore(new PersistentChatMemoryStore())
        .build();
```

`updateMessages()` 方法在每次向 `ChatMemory` 添加新的 `ChatMessage` 时都会被调用。这通常在每次与 LLM 交互期间发生两次：一次是在添加新的 `UserMessage` 时，另一次是在添加新的 `AiMessage` 时。`updateMessages()` 方法应更新与给定记忆 ID 关联的所有消息。`ChatMessage`s 可以单独存储（例如，每条消息一个记录/行/对象）或一起存储（例如，整个 `ChatMemory` 一个记录/行/对象）。

注

请注意，从 `ChatMemory` 淘汰的消息也将从 `ChatMemoryStore` 中淘汰。当一条消息被淘汰时，`updateMessages()` 方法会被调用，其消息列表中不包含被淘汰的消息。

`getMessages()` 方法在 `ChatMemory` 的用户请求所有消息时被调用。这通常在每次与 LLM 交互期间发生一次。`Object memoryId` 参数的值与创建 `ChatMemory` 时指定的 `id` 对应，可用于区分多个用户和/或对话。`getMessages()` 方法应返回与给定记忆 ID 关联的所有消息。

`deleteMessages()` 方法在调用 `ChatMemory.clear()` 时被调用。如果你不使用此功能，可以使此方法为空。



如果有多个用户，希望每个用户之间的消息隔离，可以通过给对话方法增加memoryId参数和注解，在调用对话时传入memoryId即可（类似聊天室房间号）：

```java

interface Assistant {

    String chat(@MemoryId int memoryId, @UserMessage String userMessage);
}
```



# 结构化输出

结构化输出是指将大模型返回的文本输出转为结构化的数据格式，比如一段JSON、一个对象、或是复杂的对象列表。